# 原理
## 从基础的图论开始
每条边都带有从一个顶点指向另一个顶点的方向的图为**有向图**。
定义**路径**为一系列的边，在有向图中，路径中每条边的终点一定是下一个边的起点。
如果一条路径的起点是这条路径的终点，那么这条路径就是一个**环**。
DAG，Directed Acyclic Graph，**有向无环图**，就是指没有环出现的有向图。

### 可达
当存在一条从顶点u到顶点v的路径时，顶点v被称作是从顶点u **可达**的。每个顶点都是从自身可达的（通过一条没有边的路径）。有边的路径称为 **非平凡路径**，即排除了自己连自己的情况的。从图中一个顶点出发，能到达自身的，就形成环。
因此，有向无环图也可以被定义为没有顶点可以通过非平凡路径到达自身的图。

**可达**在DAG 中是一种偏序关系，表示为：u ≤ _v_。体现在传递性（a->b, b->c, a->c) 和不可逆性（a->b, 则 b->a ，当且仅当 a=b）。

# 例子
有极其多的论文说如何用 DAG 来完成一个共识算法。这里有一个综述的入门阅读：
[BFT on a DAG](https://blog.chain.link/bft-on-a-dag/). 其实我看它等于就是把整个view 的变化用 DAG 全储存下来了。
这个好处见：
[突破区块链不可能三角](https://learnblockchain.cn/article/365)

我准备先停在这里了，显然这是极复杂的一个系统才能做到的事，Pos 的Ghost 我都理解起来很困难。
